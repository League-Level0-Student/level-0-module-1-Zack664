package _05_for_loops._2_badgers;

public class BadgerSong {
public static void main(String[] args) {

	int currentMM=0;
	int mmCount=1;
	
	for(currentMM=0; currentMM <= mmCount; currentMM +=1 ) {
	int currentB=0;
	int bCount=11;
	String Badger=("Badger");
	
	for(currentB=0; currentB <= bCount; currentB +=1 ) {
		System.out.println(Badger);
	}
	

	
	int currentM =0;
	int mCount =1;
	String Mushroom=("Mushroom");
	for(currentM=0; currentM <= mCount; currentM +=1 ) {
		System.out.println(Mushroom);
	}
	}
	int currentS =0;
	int sCount =0;
	String Snake=("Snake!");
	for(currentS=0; currentS <= sCount; currentS +=1 ) {
		System.out.println(Snake);
	}

}
}
//B=badger
//M=Mushroom